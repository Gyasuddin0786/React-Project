import React from 'react'

export default function ReactJSX() {
  return (
    <>
    <h1>React JSX</h1>
    <h3>What is JSX?</h3>
    <ul>
        <li>JSX stands for JavaScript XML.</li>
        <li>JSX allows us to write HTML in React.</li>
        <li>JSX makes it easier to write and add HTML in React.</li>
        <li>JSX allows us to write HTML elements in JavaScript and place them in the DOM without any <b>createElement()</b>  and/or <b>appendChild() </b> methods.</li>
    </ul>
    </>
  )
}

